#include <iostream>
#include <unorderd_map>
using namepsace std;
int main()
{
    unorderd_map<string, bool> dice;
    string words[] = {"apple", "banana",
                      "cherry", "dog", "elephant", "frog",
                      "grape", "hat", "ice-cream", "jacket"};
    for (int i = 0; i < sizeof(words) / sizeof(word[0]; i++))
    {
        dice.insert({word[i], true});
    }
}